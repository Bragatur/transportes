{
  "name": "transportes-braga",
  "version": "1.0.0",
  "description": "Transportes para Braga",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html dist/ && cp estacionamento.html dist/ && cp -r public dist/"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
